[package]
name = "cloudpack-graph"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
cloudpack-core = { path = "../cloudpack-core" }
petgraph = "0.6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
anyhow = "1"

[dev-dependencies]
tempfile = "3"
